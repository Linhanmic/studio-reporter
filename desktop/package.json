{
  "name": "studio-reporter-desktop",
  "version": "0.5.1",
  "private": true,
  "description": "Studio Reporter Desktop — Electron workbench for live + final reports",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test electron/discover.test.js",
    "pack": "echo \"Packaging not configured yet; use npm start for P0\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "electron": "^37.2.0"
  }
}
