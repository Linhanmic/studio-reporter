{
  "name": "studio-reporter-desktop",
  "version": "0.5.2",
  "private": true,
  "description": "Studio Reporter Desktop — Electron workbench for live + final reports",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test electron/discover.test.js electron/settings.test.js electron/compare.test.js",
    "pack": "echo \"Packaging not configured yet; use npm start for P0/P1\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "electron": "^37.2.0"
  }
}
